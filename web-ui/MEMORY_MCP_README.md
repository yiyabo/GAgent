# 🧠 Memory-MCP Web Interface

Memory-MCP (Memory with Model Context Protocol) 是AI-Driven Task Orchestration System的智能记忆管理模块,提供可视化的记忆存储、检索、管理和关系图谱展示功能。

---

## ✨ 核心功能

### 1. 📊 统计看板
实时展示记忆系统的关键指标:
- **总记忆数**: 系统中存储的所有记忆总数
- **平均连接数**: 每条记忆与其他记忆的平均语义连接数
- **嵌入覆盖率**: 已生成向量嵌入的记忆比例(用于语义搜索)
- **进化次数**: 记忆系统自我优化和重组的次数

### 2. 🔍 智能搜索与过滤
强大的多维度搜索和过滤系统:
- **全文搜索**: 支持搜索记忆内容、关键词、标签
- **类型过滤**: 按记忆类型筛选(对话、经验、知识、上下文)
- **重要性过滤**: 按重要性级别筛选(关键、高、中、低、临时)
- **防抖优化**: 搜索输入500ms防抖,减少不必要的API调用
- **组合过滤**: 支持搜索和多个过滤器同时生效

### 3. 📋 列表视图
传统表格视图,提供详细的记忆列表:
- **8列信息展示**:
  - ID (UUID截断显示)
  - 内容 (超长截断,悬停显示完整)
  - 类型 (带颜色标签)
  - 重要性 (带颜色标签)
  - 关键词 (最多显示3个)
  - 相似度 (搜索匹配度,百分比)
  - 检索次数 (被使用的次数)
  - 创建时间 (相对时间)
- **排序功能**: 支持按相似度、检索次数、时间排序
- **分页控制**: 可选10/20/50/100条每页
- **快速操作**: 一键查看详情

### 4. 🗺️ 图谱视图
创新的知识图谱可视化,展示记忆之间的语义连接:

#### 节点特征
- **大小**: 重要性基础大小 + 检索次数加成(越重要越大,越常用越大)
- **颜色**: 重要性语义颜色
  - 🔴 关键: 红色
  - 🟠 高: 橙色
  - 🔵 中: 蓝色
  - 🟢 低: 绿色
  - ⚫ 临时: 灰色
- **形状**: 记忆类型
  - ■ 对话: 方形
  - ● 经验: 椭圆
  - ◆ 知识: 菱形
  - ★ 上下文: 星形

#### 连接线特征
- **颜色和粗细**: 基于语义相似度
  - 高相似度(>80%): 绿色粗线
  - 中相似度(60-80%): 蓝色中线
  - 低相似度(<60%): 灰色细线
- **标签**: 显示精确相似度百分比

#### 交互功能
- **拖拽移动**: 拖拽节点改变位置
- **缩放平移**: 鼠标滚轮缩放,拖拽背景平移
- **点击详情**: 点击节点查看完整记忆信息
- **悬停提示**: 鼠标悬停显示记忆摘要

#### 布局算法
- **力导向布局**: 基于物理引擎,节点自然分散,适合探索连接关系
- **层次布局**: 分层排列,适合查看记忆层级结构
- **环形布局**: 环形排列,适合查看整体分布

#### 高级控制
- **搜索过滤**: 快速定位特定记忆
- **相似度阈值**: 滑块调整连接线显示阈值
- **标签切换**: 显示/隐藏节点标签
- **适应视图**: 自动缩放查看全部节点
- **全屏模式**: 沉浸式图谱体验

### 5. 💾 记忆保存
直观的表单界面用于创建新记忆:

#### 必填字段
- **记忆内容** (10-5000字符): 记忆的核心文本内容
- **记忆类型**: 选择记忆类别
  - 对话: 重要的对话内容
  - 经验: 操作经验和学习成果
  - 知识: 领域知识和概念
  - 上下文: 环境和背景信息
- **重要性级别**: 决定保留时长
  - 关键: 永久保存
  - 高: 长期保存
  - 中: 定期清理
  - 低: 短期保存
  - 临时: 自动清理

#### 可选字段
- **标签**: 多选标签,用于分类
- **关键词**: 提取的关键词,用于语义搜索
- **上下文**: 记忆产生的环境或场景
- **关联任务ID**: 关联到特定任务

#### 智能处理
- **自动嵌入生成**: 后端自动为记忆内容生成向量嵌入
- **自动关键词提取**: AI辅助提取关键词
- **语义连接**: 自动计算与其他记忆的相似度

### 6. 👁️ 记忆详情
全面的记忆详情展示:

#### 统计卡片
- 检索次数: 被搜索命中的次数
- 相似度: 当前搜索匹配度
- 连接数: 与其他记忆的连接数量

#### 基础信息
- 记忆ID (可复制)
- 记忆类型 + 说明
- 重要性级别 + 说明
- 上下文
- 关联任务

#### 内容区域
- 完整记忆内容
- 支持多行文本
- 代码块样式

#### 元数据
- 标签集合
- 关键词集合
- 记忆连接列表(带相似度)
- 时间信息(创建/最后访问)

---

## 🏗️ 技术架构

### 前端技术栈
- **React 18**: 现代UI库,支持并发特性
- **TypeScript**: 类型安全,IDE友好
- **Ant Design 5**: 企业级UI组件库
- **Zustand**: 轻量级状态管理
- **React Query**: 服务器状态管理,自动缓存
- **vis-network**: 强大的图谱可视化库
- **dayjs**: 日期处理库

### 架构设计

#### 文件结构
```
web-ui/src/
├── api/
│   └── memory.ts                 # Memory API客户端
├── components/
│   ├── common/
│   │   └── ErrorBoundary.tsx     # 错误边界组件
│   └── memory/
│       ├── MemoryGraph.tsx       # 图谱可视化组件
│       ├── MemoryDetailDrawer.tsx # 详情抽屉组件
│       └── SaveMemoryModal.tsx   # 保存表单组件
├── pages/
│   └── Memory.tsx                # 主页面组件
├── store/
│   └── memory.ts                 # Memory状态管理
└── types/
    └── index.ts                  # TypeScript类型定义
```

#### 组件职责
- **Memory.tsx**: 主页面,协调所有子组件,处理路由和布局
- **MemoryGraph.tsx**: 独立的图谱可视化,可复用
- **MemoryDetailDrawer.tsx**: 记忆详情展示,无业务逻辑
- **SaveMemoryModal.tsx**: 表单管理,包含验证和提交
- **ErrorBoundary.tsx**: 错误捕获和降级显示

#### 状态管理
```typescript
// Zustand Store 结构
interface MemoryState {
  memories: Memory[];              // 记忆列表
  selectedMemory: Memory | null;   // 当前选中的记忆
  stats: MemoryStats | null;       // 统计信息
  filters: FilterState;            // 过滤条件
  loading: boolean;                // 加载状态
  error: string | null;            // 错误信息
  // Actions
  setMemories: (memories: Memory[]) => void;
  addMemory: (memory: Memory) => void;
  updateMemory: (id: string, updates: Partial<Memory>) => void;
  removeMemory: (id: string) => void;
  setFilters: (filters: Partial<FilterState>) => void;
  clearFilters: () => void;
  getFilteredMemories: () => Memory[];
}
```

#### API设计
```typescript
class MemoryApi extends BaseApi {
  // 保存新记忆
  async saveMemory(request: SaveMemoryRequest): Promise<Memory>

  // 查询记忆
  async queryMemory(request: QueryMemoryRequest): Promise<QueryMemoryResponse>

  // 获取统计信息
  async getStats(): Promise<MemoryStats>

  // 自动保存任务记忆
  async autoSaveTask(taskId: number, taskName: string, content: string): Promise<{success: boolean; memory_id: string}>
}
```

---

## 🎨 设计原则

### KISS (Keep It Simple, Stupid)
- 界面布局清晰,功能分区明确
- 操作流程简单,用户学习成本低
- 代码结构扁平,避免过度抽象

### DRY (Don't Repeat Yourself)
- 颜色映射函数复用(getNodeColor, getMemoryTypeColor)
- 通用的ErrorBoundary组件
- BaseApi继承模式

### SOLID原则
- **单一职责**: 每个组件只负责一件事
  - Memory.tsx: 页面布局和协调
  - MemoryGraph.tsx: 图谱可视化
  - SaveMemoryModal.tsx: 表单管理
- **开放封闭**: 通过props扩展组件功能,不修改内部实现
- **依赖倒置**: 组件依赖接口(Memory类型),不依赖具体实现

### 性能优化
- **React.memo**: 防止不必要的重渲染
- **useCallback**: 缓存事件处理函数
- **useMemo**: 缓存计算结果
- **搜索防抖**: 减少API调用频率
- **React Query缓存**: 自动管理服务器状态缓存
- **虚拟滚动**: 大量数据时使用(未来)

### 用户体验
- **Loading状态**: 所有异步操作都有loading提示
- **错误处理**: 友好的错误提示和重试机制
- **空状态**: 无数据时显示引导信息
- **响应式设计**: 适配不同屏幕尺寸
- **可访问性**: 支持键盘导航和屏幕阅读器

---

## 🚀 使用指南

### 快速开始

#### 1. 启动后端服务
```bash
cd /path/to/backend
python main.py
```

#### 2. 启动前端服务
```bash
cd web-ui
npm install
npm run dev
```

#### 3. 访问页面
浏览器打开 `http://localhost:5173`,点击侧边栏"记忆管理"菜单项

### 常见使用场景

#### 场景1: 保存重要对话
1. 点击"保存新记忆"按钮
2. 选择类型"对话",重要性"高"
3. 填写对话内容
4. 添加标签: "用户反馈", "功能需求"
5. 点击保存

#### 场景2: 查找相关记忆
1. 在搜索框输入关键词
2. 选择记忆类型过滤
3. 查看列表或切换到图谱视图
4. 点击节点/行查看详情

#### 场景3: 分析记忆关系
1. 切换到"图谱视图"标签
2. 选择"力导向"布局
3. 拖拽节点探索连接
4. 调整相似度阈值过滤弱连接
5. 点击节点查看详情

#### 场景4: 监控记忆系统
1. 查看顶部统计看板
2. 检查嵌入覆盖率(应 >80%)
3. 查看平均连接数(健康值 >2)
4. 定期检查进化次数

---

## 🔧 配置与扩展

### 环境变量
```env
# .env
VITE_API_BASE_URL=http://localhost:8000  # 后端API地址
VITE_API_TIMEOUT=30000                    # API超时时间(ms)
```

### 自定义配置

#### 修改搜索防抖时间
```typescript
// Memory.tsx
const debouncedSearchQuery = useDebounce(searchInput, 1000); // 改为1秒
```

#### 修改刷新间隔
```typescript
// Memory.tsx
const { data: stats } = useQuery({
  queryKey: ['memory-stats'],
  queryFn: () => memoryApi.getStats(),
  refetchInterval: 60000, // 改为1分钟
});
```

#### 修改图谱物理引擎
```typescript
// MemoryGraph.tsx
physics: {
  barnesHut: {
    gravitationalConstant: -5000,  // 调整引力
    springLength: 200,             // 调整弹簧长度
    springConstant: 0.05,          // 调整弹簧常数
  },
}
```

---

## 🐛 故障排查

### 常见问题

#### 1. 页面空白
**原因**: ErrorBoundary捕获了错误
**解决**: 打开浏览器控制台查看错误详情,检查后端API是否正常

#### 2. 统计信息不显示
**原因**: 后端API返回错误或数据格式不匹配
**解决**:
- 检查 `/mcp/memory/stats` 接口是否正常
- 检查后端返回的数据结构是否符合 `MemoryStats` 类型

#### 3. 图谱不显示节点
**原因**: 记忆数据为空或过滤条件太严格
**解决**:
- 调低相似度阈值
- 清除搜索和过滤条件
- 检查是否有记忆数据

#### 4. 搜索无结果
**原因**: 搜索关键词不匹配或防抖未完成
**解决**:
- 等待500ms让防抖完成
- 检查搜索关键词是否存在于content/keywords/tags中

#### 5. 保存记忆失败
**原因**: 表单验证失败或后端API错误
**解决**:
- 检查所有必填项是否填写
- 检查内容长度是否在10-5000字符之间
- 查看错误消息定位问题

---

## 📈 性能指标

### 目标性能
- 首屏加载: < 2s
- 列表渲染(100条): < 500ms
- 图谱渲染(50节点): < 1s
- 搜索响应: < 300ms (防抖后)
- 页面切换: < 200ms

### 性能监控
```typescript
// 使用Performance API
performance.mark('graph-render-start');
// ... 渲染逻辑
performance.mark('graph-render-end');
performance.measure('graph-render', 'graph-render-start', 'graph-render-end');
const measure = performance.getEntriesByName('graph-render')[0];
console.log('Graph render time:', measure.duration, 'ms');
```

---

## 🔮 未来规划

### 短期(1-2月)
- [ ] 记忆编辑功能
- [ ] 批量删除功能
- [ ] 导出为JSON/CSV
- [ ] 高级搜索(日期范围、正则表达式)

### 中期(3-6月)
- [ ] 记忆版本历史
- [ ] 记忆标注和评分
- [ ] 自定义图谱布局
- [ ] 记忆推荐系统

### 长期(6-12月)
- [ ] AI辅助记忆整理
- [ ] 多维度记忆分析
- [ ] 协作式记忆管理
- [ ] 移动端适配

---

## 📝 开发指南

### 添加新功能

#### 1. 定义类型
```typescript
// types/index.ts
export interface NewFeature {
  id: string;
  name: string;
}
```

#### 2. 添加API
```typescript
// api/memory.ts
async getNewFeature(): Promise<NewFeature> {
  return this.get<NewFeature>('/mcp/new_feature');
}
```

#### 3. 更新Store
```typescript
// store/memory.ts
interface MemoryState {
  // ...
  newFeature: NewFeature | null;
  setNewFeature: (feature: NewFeature) => void;
}
```

#### 4. 创建组件
```typescript
// components/memory/NewFeatureComponent.tsx
const NewFeatureComponent: React.FC = () => {
  // ...
};
```

#### 5. 集成到页面
```typescript
// pages/Memory.tsx
import NewFeatureComponent from '@components/memory/NewFeatureComponent';
// 在适当位置渲染组件
```

---

## 🤝 贡献指南

### 代码规范
- 遵循TypeScript严格模式
- 使用ESLint和Prettier
- 编写有意义的注释
- 遵循SOLID原则

### 提交规范
```
feat: 添加记忆编辑功能
fix: 修复图谱渲染性能问题
docs: 更新README文档
style: 调整按钮样式
refactor: 重构API客户端
test: 添加Memory组件测试
```

---

## 📄 许可证

MIT License

---

## 📮 联系方式

- **项目仓库**: https://github.com/yiyabo/GAgent
- **问题反馈**: 提交GitHub Issue
- **功能建议**: 提交GitHub Discussion

---

**最后更新**: 2025-10-13
**版本**: v1.0.0
