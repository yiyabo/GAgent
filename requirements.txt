fastapi>=0.111.0
starlette>=0.37.2
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
httpx>=0.23.0
socksio>=1.0.0
requests>=2.31.0
aiohttp>=3.8.0
# vision_reader 的 PDF file-extract 路径（按页计费）唯一使用者；此前未声明，
# 容器里没装 → 该路径一直 ImportError（扫描件读不了），2026-09-26 补声明。
# 锁 1.x：代码用的是 v1 风格客户端（client.files.create / chat.completions.create），
# 且 1.x 复用镜像里已有的 httpx；3.x 会再拉一套 httpx2/httpcore2/truststore。
openai>=1.40.0,<2.0.0
numpy>=1.26.0
pandas>=2.0.0
networkx>=3.0.0
docker>=6.0.0
scipy>=1.10.0
PyYAML>=6.0.0
sentence-transformers>=2.7.0
torch>=2.0.0
psutil>=5.9.0
pypdf>=3.0.0
Pillow>=10.0.0
reportlab>=4.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
markdown>=3.5.0
asyncssh>=2.14.2
redis>=5.0.0
argon2-cffi>=23.1.0
mammoth>=1.6.0
email-validator>=2.0.0
pyahocorasick>=2.0.0
