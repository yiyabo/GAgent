uploads/ 目录有订单数据 orders3.csv（列：订单号,状态,金额；状态为 已签收/已发货/待发货/已取消）。
请完成三步：
1. 按状态统计订单数和金额合计，保存 results/status_summary.csv（列：状态,订单数,金额合计，
   金额保留两位小数，按订单数从高到低，不要输出索引列）。
2. 画一张各状态订单数占比的饼图，保存 deliverables/status_pie.png。
3. 写一份分析报告 deliverables/order_report.md（不少于 600 字，包含 ## 数据概览、
   ## 状态分析、## 结论 三个小节），必须给出"已签收"订单的数量及其金额合计。
