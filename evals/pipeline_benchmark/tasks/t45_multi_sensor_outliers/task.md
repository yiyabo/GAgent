uploads/ 目录有 20 天的温度监测 temp.csv（列：时间,温度，每小时一条）。请完成三步：
1. 用 1.5×IQR 规则找出温度离群点，保存 results/temp_outliers.csv（列：时间,温度，
   不要输出索引列）。
2. 画一张温度随时间变化的折线图，保存 deliverables/temp_line.png。
3. 写一份分析报告 deliverables/temp_report.md（不少于 600 字，包含 ## 数据概览、
   ## 异常分析、## 结论 三个小节），必须给出离群点的数量。
