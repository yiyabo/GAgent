请用 execute_code 批量获取并汇总 3 条蛋白质序列（在 kernel 里循环调用 sequence_fetch，不要逐个手动调）：

1. P04637（p53）
2. P38398（BRCA1）
3. P61626（lysozyme）

处理逻辑：
- 获取每条序列并记录序列长度（氨基酸数）；
- 只保留长度 > 200 的序列；
- 汇总：保留下来的序列 id 及各自长度、被过滤掉的 id、保留序列长度的均值（保留一位小数）。

把每个 id 的长度、保留/过滤结论、以及长度均值都写在回复里。
