uploads/measurements.csv 有 40 行两列数据（x, y）。

请用 execute_code 完成科学计算——kernel 里可以正常 import numpy / pandas / math / statistics 等科学计算与标准库包（只有后端内部包被守卫拦截，正常科学计算不受影响）：
1. 计算 x 与 y 的 Pearson 相关系数（保留三位小数）；
2. 计算 y 的均值（保留两位小数）；
3. 对 (x, y) 做最小二乘线性拟合，给出斜率（保留三位小数）。

把这三个数值写在回复里。
